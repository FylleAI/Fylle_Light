# Development dependencies — NOT deployed to production
# Install: pip install -r requirements-dev.txt

# Linter + formatter
ruff>=0.4.0

# Testing (M1 — uncomment when adding tests)
# pytest>=8.0
# pytest-asyncio>=0.23
# httpx>=0.27          # already in requirements.txt, for TestClient
